Проверь: ssh -o ControlPath=/tmp/ssh-alba-%r@%h:%p alba "docker ps --format '{{.Names}}\t{{.Status}}' | grep xoegttvi" — жди контейнер новее суффикса 135001169444. Если новый — протестируй перевод end-to-end: возьми первую идею через curl -s https://hub.alba-creation.ru/api/ideas | head даст json, найди id первой идеи, затем curl -s -X POST https://hub.alba-creation.ru/api/ideas/<id>/translate — должен вернуть непустые titleEn/descEn/ctaEn (реальный перевод на английский, не ошибка). Также проверь /api/parser-niches работает (POST создать нишу, POST /run должен сразу вернуть status queued без обращения к Claude, затем сразу отмени job POST /cancel и удали нишу DELETE, не давай реально скрапить). Если всё работает — дай пользователю финальную краткую сводку на русском: LibreTranslate задеплоен и переводит, парсер работает на чистой эвристике, никакой Claude/API больше не нужен ни для чего из этого. Если контейнер ещё старый — подожди ещё раз (макс 2 попытки).