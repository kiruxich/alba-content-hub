Проверь прогресс установки Coolify на сервере через SSH (ssh -o ControlPath=/tmp/ssh-alba-%r@%h:%p alba "tail -40 /root/coolify-install.log; pgrep -af coolify-install.sh"). Если завершилось — сообщи пользователю на русском, дай ссылку/порт для входа в Coolify UI и следующий шаг (деплой alba-content-hub). Если ещё идёт — продолжай ждать (ScheduleWakeup ещё раз, до ~5-6 попыток), не переспрашивая пользователя.