Проверь, что автодеплой hub (alba-content-hub) на Coolify прошёл успешно после пуша коммита 353b4e2: ssh -o ControlPath=/tmp/ssh-alba-%r@%h:%p alba "docker ps --format '{{.Names}}\t{{.Status}}' | grep xoegttvi" (ожидай новый суффикс контейнера/время старта), затем curl -s -o /dev/null -w "%{http_code}\n" https://hub.alba-creation.ru/api/parser-niches. Если всё ок — продолжай автономно (пользователь ушёл, разрешил продолжать без подтверждений) со следующим этапом: установка инфраструктуры parser-worker на VPS (Xvfb, Python3, Playwright+Chromium, systemd unit для parser-worker на порту 8787, DISPLAY=:99). Не жди дальнейших указаний, сразу приступай к SSH-командам на сервере alba. Если деплой ещё не прошёл — подожди ещё раз (максимум 2-3 попытки), потом продолжай той же цепочкой действий.