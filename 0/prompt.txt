Проверь: dig hub.alba-creation.ru A +short @ns1.reg.ru. Если вернулся 80.78.244.31 — сообщи пользователю на русском и настрой nginx+SSL для hub.alba-creation.ru через SSH на сервере alba (по тому же паттерну, что уже сделан для coolify.alba-creation.ru: certbot certonly --nginx -d hub.alba-creation.ru, затем nginx-конфиг с proxy_pass на http://127.0.0.1:3001, systemctl reload nginx), затем проверь curl https://hub.alba-creation.ru. Также добавь этот же сайт (hub) в vars/main.yml ansible-плейбука проекта alba-content-hub (infra/ansible/vars/main.yml) для консистентности. Если DNS ещё не появился — сообщи пользователю что всё ещё ждём и предложи подождать ещё, не зацикливайся сама на повторных ScheduleWakeup без ведома пользователя.