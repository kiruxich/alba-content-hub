Проверь: ssh -o ControlPath=/tmp/ssh-alba-%r@%h:%p alba "docker ps --format '{{.Names}}\t{{.Status}}' | grep xoegttvi" — жди контейнер новее суффикса 154602726953. Если новый — проверь curl -s -X POST https://hub.alba-creation.ru/api/ideas/1787931890063/translate — должен вернуть непустые titleEn/descEn (реальный перевод, не {"error":"fetch failed"}). Если работает — сообщи пользователю на русском: полностью микросервисная архитектура готова, всё через Coolify (hub, alba-creation, LibreTranslate, parser-worker), дай краткую финальную сводку что было сделано с самого начала темы про микросервисы. Если ошибка сохраняется — задиагностируй (проверь env var применился, проверь connectivity с 10.0.1.1:5000 изнутри hub-контейнера) и почини. Если контейнер ещё старый — подожди ещё раз (макс 2 попытки).